/**
 * This package holds the ODVExporter plugin.
 * This plugin uses all available plugins to export the data
 * and groups the export files within a ZIP-archive.
 */
package de.opendiabetes.vault.plugin.exporter.ODVExporter;
